// Este arquivo é substituído automaticamente pelo vite-plugin-pwa em produção.
// Mantido aqui apenas como fallback de desenvolvimento.
self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', () => self.clients.claim());
